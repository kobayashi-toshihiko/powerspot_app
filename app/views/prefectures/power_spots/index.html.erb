<div class="container mt-5">
  <div class="row justify-content-between">
    <div class="col-md-3 mt-5">
      <p class="text-main fw-bold"></p>
      <!--新規投稿順サイドバー-->
      <%= render 'layouts/sidebar_posts' %>
    </div>
    
    <div class="col-md-9 d-flex justify-content-between mt-3">
      <div class="col-md-11 ms-4">
        <div class="row">

          <!--都道府県別パワースポット一覧-->
            <% @power_spots.each do |power_spot| %>
            <section id="attribute_compatibility">
              <h5 class="mt-5 border-bottom border-primary p-2"><%= @prefecture.name %>のパワースポット記事一覧</h5>
              <div class="d-block border border-primary mt-5">
                <h5 class="p-2 btn-primary fw-bold" style="margin-bottom: 0px;"><%= link_to power_spot.main_title, prefecture_power_spot_path(@prefecture, power_spot), class: 'text-white', style: 'text-decoration:none;'%></h5>
              </div>
              <!--<p class="mt-2">2019年4月25日    [<a href="#">北海道</a>]</p>-->
              <p class="mt-2"><%= power_spot.created_at.strftime("%Y年%m月%d日") %>    [<%= link_to @prefecture.name, prefecture_power_spots_path(@prefecture) %>]</p>
              <div class="row mt-3">
              </div>
            </section>
            
            <section id="attribute_compatibility">
              <div class="row mt-2 mb-3">
                <div class="col-md-4 mt-1">
                  <!--<img src='status.png'>-->
                  <%#= image_tag 'LakeMashu1.jpg', class: 'w-100 d-block carousel-item-image', alt: 'LakeMashu1' %>
                  <%= image_tag power_spot.image1_url, class: 'w-100 d-block carousel-item-image', alt: 'LakeMashu1' %>
                </div>
                <div class="col-md-8">
                  <div class="mb-3">
                    <!--<p>基本的に、隣り合った属性は相性が悪く、隣り合った属性のパワースポットに行くと、利益を受けにくいと言われています。以下の相性早見表であなたと相性が良い属性と相性が悪い属性を確認してみて下さい！</p>-->
                    <p><%= power_spot.main_descript[0..50].concat('・・・') %></p>
                  </div>
                </div>
              </div>
            <%= link_to "→#{power_spot.main_title}の続きを読む", prefecture_power_spot_path(@prefecture, power_spot), class: 'mt-4' %>
            </section>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>