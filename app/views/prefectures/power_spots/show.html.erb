<div class="container mt-5">
  <div class="row justify-content-between">
    <div class="col-md-3 mt-5">
      <p class="text-main fw-bold"></p>
      <!--新規投稿順サイドバー-->
      <%= render 'layouts/sidebar_posts' %>
    </div>
    
    <div class="col-md-9 d-flex justify-content-between mt-3">
      <div class="col-md-11 ms-4">
        <div class="row">
          <!--都道府県別パワースポット一覧-->
          <% @power_spots.each do |power_spot| %>
            <section id="attribute_compatibility">
              <div class="d-block border border-primary mt-5">
                <h5 class="p-2 btn-primary fw-bold" style="margin-bottom: 0px;"><%= link_to "【#{power_spot.name}】#{power_spot.main_title}【#{@prefecture.name}】", prefecture_power_spot_path(@prefecture, power_spot), class: 'text-white', style: 'text-decoration:none;'%></h5>
              </div>
              <!--<p class="mt-2">2019年4月25日    [<a href="#">北海道</a>]</p>-->
              <p class="mt-2">
                <%= power_spot.created_at.strftime("%Y年%m月%d日") %>
                [<%= link_to @prefecture.name, prefecture_power_spots_path(@prefecture) %>]
                [<%= link_to "#{power_spot.element.name}属性", prefecture_power_spots_path(@prefecture) %>]
              </p>
              <!--<div class="row"></div>-->
              <div class="pl-2 d-block border border-primary my-4" style="padding-left: 15px;">
                <h5 class="pt-2 "><%= power_spot.sub_title %></h5>
              </div>
              <div class="row mt-2 mb-3">
                <div class="col-md-4 mt-1">
                  <!--<img src='status.png'>-->
                  <%= image_tag power_spot.image1_url, class: 'w-100 d-block carousel-item-image', alt: 'LakeMashu1' %>
                </div>
                <div class="col-md-8">
                  <div class="mb-3">
                    <!--<p>基本的に、隣り合った属性は相性が悪く、隣り合った属性のパワースポットに行くと、利益を受けにくいと言われています。以下の相性早見表であなたと相性が良い属性と相性が悪い属性を確認してみて下さい！</p>-->
                    <p><%= power_spot.main_descript %></p>
                  </div>
                </div>
              </div>

              <div class="pl-2 d-block border border-primary mt-5 mb-4" style="padding-left: 15px;">
                <h5 class="pt-2 "><%= "#{power_spot.name}のご利益は？" %></h5>
              </div>
              <div class="row mt-2 mb-3">
                <div class="col-md-4 mt-1">
                  <!--<img src='status.png'>-->
                  <%= image_tag power_spot.image1_url, class: 'w-100 d-block carousel-item-image', alt: 'LakeMashu1' %>
                </div>
                
                <div class="col-md-8">
                  <div class="mb-3">
                 <% power_spot.blessing_descript.split("。").each do |descript| %>
                    <!--<p class="fs-5"><%#= power_spot.access_info %></p>-->
                    <p class="mb-3"><%= descript %></p>
                    
                  <% end %>

                    <!--<p>基本的に、隣り合った属性は相性が悪く、隣り合った属性のパワースポットに行くと、利益を受けにくいと言われています。以下の相性早見表であなたと相性が良い属性と相性が悪い属性を確認してみて下さい！</p>-->
                  </div>
                </div>
              </div>

              <div class="pl-2 d-block border border-primary mt-5 mb-4" style="padding-left: 15px;">
                <h5 class="pt-2 "><%= "#{power_spot.name}の属性は？" %></h5>
              </div>

              <div class="col-md-12">
                <div class="p-2 mb-3">
                  <p><%= "#{power_spot.name}のパワースポット属性は " %>
                    <span class="fw-bold fs-5">「</span>
                    <span class= "fw-bold fs-5">
                      <%= power_spot.element.name %>
                    </span>
                    <span class="fw-bold fs-5 <%= class_color(power_spot.element.name) %>">」</span>属性です。
                  </p>
                  <p>自分の属性が何か知りたい場合はこちらで診断して見て下さいね!</p>
                </div>
              </div>

              <!--属性相性早見表-->
              <section id="attribute_compatibility">
                <div class="row mt-5">
                  <div class="col-md-6">
                    <!--<img src='status.png'>-->
                    <%= image_tag 'status.png', class: 'w-100 d-block carousel-item-image', alt: 'status' %>
                  </div>
                  <div class="col-md-5 mt-2" style="margin-left: 50px;">
                    <div class="mb-4">
                      <p>基本的に、隣り合った属性は相性が悪く、隣り合った属性のパワースポットに行くと、利益を受けにくいと言われています。以下の相性早見表であなたと相性が良い属性と相性が悪い属性を確認してみて下さい！</p>
                    </div>
                    <!--<b>相性早見表</b>-->
                    <table class="table table-bordered border-secondary">
                      <tr class="table-primary text-center">
                        <td>あなたの属性</td><td>相性良い</td><td>相性悪い</td>
                      </tr>
                      <tr class="text-center">
                        <td class="table-primary">空</td><td>空水火</td><td>地風</td>
                      </tr>
                      <tr class="text-center">
                        <td class="table-primary">地</td><td>地火風</td><td>水空</td>
                      </tr>
                      <tr class="text-center">
                        <td class="table-primary">水</td><td>水風空</td><td>火地</td>
                      </tr>
                      <tr class="text-center">
                        <td class="table-primary">火</td><td>火空地</td><td>風水</td>
                      </tr>
                      <tr class="text-center">
                        <td class="table-primary">風</td><td>風地水</td><td>空火</td>
                      </tr>                    
                    </table>
                  </div>
                </div>
              </section>

              <div class="pl-2 d-block border border-primary mt-5 mb-2" style="padding-left: 15px;">
                <h5 class="pt-2"><%= "#{power_spot.name}へのアクセスは？" %></h5>
              </div>
              <div class="col-md-12 p-2">
                <div class="p-2 mb-3">
                  <% power_spot.access_info.split("。").each do |access| %>
                    <!--<p class="fs-5"><%#= power_spot.access_info %></p>-->
                    <p class="mb-3"><%= access %></p>
                  <% end %>
                  <!--GoogleMapを表示-->
                </div>
                  <%= power_spot.address.html_safe %>
              </div>
            </section>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>